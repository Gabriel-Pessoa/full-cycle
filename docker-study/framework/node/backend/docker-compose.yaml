version: '3'

services:
  app:
    build:
      context: ./
    container_name: app
    networks:
      - node-network
    volumes:
      - ./:/usr/src/app
    tty: true
    ports:
      - "3000:3000"
    networks:
      - node-network

networks:
  node-network:
    external: true